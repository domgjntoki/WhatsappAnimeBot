import requests
import re


def command_voice_actor_by_character(driver, chat, wpp_message):
    print()


def command_voice_actor_by_name(driver, chat, wpp_message):
    print()


def voice_actor_resume(va_id):
    print()
